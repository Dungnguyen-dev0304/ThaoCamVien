@model IEnumerable<SharedThaoCamVien.Models.QrCode>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QrCodeData)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Poi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QrCodeData)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Poi.Description)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.QrId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.QrId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.QrId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
